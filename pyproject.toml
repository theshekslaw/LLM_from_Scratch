[project]
name = "llm-backend"
version = "0.1.0"
description = "LLM implementation from scratch using PyTorch"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["torch", "tiktoken", "matplotlib"]

[project.optional-dependencies]
pretrained = ["transformers"]
